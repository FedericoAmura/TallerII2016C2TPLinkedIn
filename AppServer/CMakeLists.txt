cmake_minimum_required(VERSION 3.2.2)
project(AppServer)
include_directories(include)
file(GLOB_RECURSE SOURCES "src/*.cpp" "src/*.c")
add_executable(AppServer ${SOURCES} )
TARGET_LINK_LIBRARIES(AppServer pthread)
